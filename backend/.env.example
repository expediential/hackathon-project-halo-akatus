# SQLite is the local default. PostgreSQL URLs work without model changes.
DATABASE_URL=sqlite:///./halo.db
OUTDATED_THRESHOLD_MINUTES=60
INCIDENT_MATCH_THRESHOLD=0.62
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
